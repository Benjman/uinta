function (app name)
    message(STATUS "Adding ${CMAKE_CURRENT_LIST_DIR}/${name}")
    add_executable(${name} ./${name}/main.cpp)
    target_link_libraries(${name} PRIVATE uinta)
    target_include_directories(${name} PRIVATE ${GL_INCLUDES})
endfunction()

app(camera3d)
app(font)
app(model)
app(quadtree)
app(ray_picking)
app(terrain)
