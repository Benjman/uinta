#ifndef UINTA_CFG_H
#define UINTA_CFG_H

// cmake generates this implementation. edits should be made to uinta/cmake/cfg.hpp.in

const inline char* RES_PATH = "${UINTA_RES_PATH}";
const inline char* LIB_PATH = "${UINTA_LIB_PATH}";

#endif // UINTA_CFG_H
