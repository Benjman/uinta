cmake_minimum_required(VERSION 3.15)

project(UintaPlatform
        VERSION 0.1)

set(UINTA_PLATFORM_INCLUDE ${CMAKE_CURRENT_SOURCE_DIR}/include)
set(UINTA_PLATFORM_INCLUDE ${UINTA_PLATFORM_INCLUDE} PARENT_SCOPE)

set(INCLUDES ${INCLUDES} ${UINTA_PLATFORM_INCLUDE} PARENT_SCOPE)

include_directories(UintaPlatform ${UINTA_PLATFORM_INCLUDE})
