#include <gtest/gtest.h>

#define private public
#include "uinta/font.h"

namespace uinta {

// FIXME:
TEST(TextMeshBuilder, alignment) {
  // TextMeshBuilder builder(nullptr, "");
  // ASSERT_TRUE(builder.isTopAligned());
  // ASSERT_TRUE(builder.isLeftAligned());
  //
  // builder.align(Alignment::TopCenter);
  // ASSERT_TRUE(builder.isTopAligned());
  // ASSERT_TRUE(builder.isCenterAligned());
  //
  // builder.align(Alignment::TopRight);
  // ASSERT_TRUE(builder.isTopAligned());
  // ASSERT_TRUE(builder.isRightAligned());
  //
  // builder.align(Alignment::MiddleLeft);
  // ASSERT_TRUE(builder.isMiddleAligned());
  // ASSERT_TRUE(builder.isLeftAligned());
  //
  // builder.align(Alignment::MiddleCenter);
  // ASSERT_TRUE(builder.isMiddleAligned());
  // ASSERT_TRUE(builder.isCenterAligned());
  //
  // builder.align(Alignment::MiddleRight);
  // ASSERT_TRUE(builder.isMiddleAligned());
  // ASSERT_TRUE(builder.isRightAligned());
  //
  // builder.align(Alignment::BottomLeft);
  // ASSERT_TRUE(builder.isBottomAligned());
  // ASSERT_TRUE(builder.isLeftAligned());
  //
  // builder.align(Alignment::BottomCenter);
  // ASSERT_TRUE(builder.isBottomAligned());
  // ASSERT_TRUE(builder.isCenterAligned());
  //
  // builder.align(Alignment::BottomRight);
  // ASSERT_TRUE(builder.isBottomAligned());
  // ASSERT_TRUE(builder.isRightAligned());
}

}  // namespace uinta
