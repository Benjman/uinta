{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Engine tests",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/src/app/app",
      "cwd": "${workspaceFolder}"
    }
  ]
}
